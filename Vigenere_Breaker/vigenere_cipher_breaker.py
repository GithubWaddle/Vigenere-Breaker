# breaking a vigenere cipher without knowing the key


def break(ciphertext: str) -> str:
	plaintext: str = ""
	return plaintext


# for visualization of execution on website frontend
def break_with_polling(ciphertext: str) -> str:
	plaintext: str = ""
	return plaintext