# for use in normally encoding or decoding vigenere ciphers


def encrypt(plaintext: str, key: str) -> str:
	ciphertext: str = ""
	return ciphertext


def decrypt(ciphertext: str, key: str) -> str:
	plaintext: str = ""
	return plaintext